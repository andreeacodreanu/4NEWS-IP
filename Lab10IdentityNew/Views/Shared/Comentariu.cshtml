@model ProiectIP.Models.Comment

<div class="entity_comments">
    <div class="entity_inner__title">
        <h2>Add a Comment</h2>
    </div>
    <!--Entity Title -->


    <div class="entity_comment_from">
        @if (ViewBag.afisareButoaneComentariu == true )
        {
            <form method="post" action="/Article/Show">
                @*<div class="form-group">
                    <input type="text" class="form-control" id="inputName" placeholder="Name">
                </div>
                <div class="form-group">
                    <input type="text" class="form-control" id="inputEmail" placeholder="Email">
                </div>*@
                <div class="form-group comment">
                    @*<textarea class="form-control" id="inputComment" placeholder="Comment"></textarea>*@
                    @Html.HiddenFor(m => m.UserId)

                    @Html.ValidationSummary(false, "", new { @class = "text-danger" })

                    <br /><br />
                    @Html.Label("CommentContent", "Comment")
                    <br />
                    @Html.TextArea("CommentContent", null, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.CommentContent, "Continutul este obligatoriu", new { @class = "text-danger " })
                    <br /><br />
                    @Html.Hidden("CommentDate", DateTime.Now, new { @class = "form-control" })
                    <br /><br />
                    @Html.HiddenFor(m => m.ArticleId)
                </div>

                <button type="submit" class="btn btn-pink">Submit</button>
            </form>
        }
        else
        {<a href="/Account/Login">Log in</a>}
        </div>
    <!--Entity Comments From -->

</div>
<!--Entity Comments -->
